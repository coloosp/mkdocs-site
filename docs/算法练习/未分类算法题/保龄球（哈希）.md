# 保龄球（哈希）
[保龄球](https://www.luogu.com.cn/problem/P1918)
额...就是这样，STL太厉害了，二分查找根本不是对手
```
#include <iostream>
# include <vector>
# include <queue>
# include <unordered_map>
// #include <bits/stdc++.h>
using namespace std;

int main() {
	int n;
	cin >> n;
	unordered_map <int, int> a;
	for (int i = 1; i <=n; i++) {
		int b;
		cin >> b;
		a[b] = i;
	}
	int q;
	cin >> q;
	while (q--) {
		int m;
		cin >> m;
			cout << a[m] << endl;
	}







	return 0;
}
```