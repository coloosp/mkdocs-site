# 两个数组的交集
[两个数组的交集](https://leetcode.cn/problems/intersection-of-two-arrays/description/)
因为题目要求返回的元素是唯一的，所以这里使用set来存储会方便很多（开始我打算返回结果是vector，但是这样还需要擦除重复的元素）
## 代码
```
class Solution {
public:
    vector<int> intersection(vector<int>& nums1, vector<int>& nums2) {
        unordered_set <int> nums;
        unordered_set <int> result;
        for(int i=0;i<nums1.size();i++){
            nums.insert(nums1[i]);
        }
        for(int i=0;i<nums2.size();i++){
            if(nums.find(nums2[i])!=nums.end()){
                result.insert(nums2[i]);
            }
        }
        
        return vector <int> (result.begin(),result.end());




    }
};
```
## 解析
嗯，毕竟是简单题，稍微理解一下STL的知识就相当容易了
另外提一句，这里当然也可以使用数组来来作为存储，但是开一个1000大小的数组，可能不必set的开销小（实际上我也不知道哪个开销大），各位还是慎重选择