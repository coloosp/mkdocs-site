# 全排列
[全排列](https://leetcode.cn/problems/permutations/?envType=study-plan-v2&envId=top-100-liked)

终于到了回溯的章节，我认为回溯是相当套路的题型，虽然初见者可能认为这很困难，但是掌握套路后并不麻烦。
回溯的区分可能在于其中筛选机制的区别，对于组合，排列，是否可以重复都有区分，各位在后面的题目中可以理解这些

具体回溯的细节作者已经在这里详细讲过了，如果还不了解回溯如何实现的朋友建议先去看看[回溯杂谈](https://coloosp.github.io/mkdocs-site/算法杂谈/backtrack/)

然后要提的是回溯章节我已经不太会标注时间复杂度，因为作者也不算来（

## 解析
不过我们还是先来认识一下回溯递归三步在这里的使用吧
1. 确定参数：有数组和标记数组，这里不需要index确定，只需要标记数组即可
2. 确定终止条件：path收集满，即等于nums.size()
3. 单层逻辑：遍历nums，加入其中未标记的元素

这里分享一下作者理解的回溯小技巧，各位在哪里处理的逻辑(比如节点加入与标记)，就在对称位置反向恢复现场

## 代码
```
class Solution {
public:
    vector<vector<int>> result;
    vector<int> path;

    void backtracking(vector<int>& nums,vector<int>&used)
    {
        if(path.size()==nums.size()) //满足条件就可以收集
        {        
            result.push_back(path);
            return;
        }

        for(int i=0;i<nums.size();i++)
        {
            if(used[i]) //收集过的就跳过
                continue;
            used[i]=1;
            path.push_back(nums[i]);
            backtracking(nums,used);
            used[i]=0;
            path.pop_back();
        }
    }

    vector<vector<int>> permute(vector<int>& nums) {
        vector <int> used(nums.size(),0);
        backtracking(nums,used);

        return result;

    }
};
```
